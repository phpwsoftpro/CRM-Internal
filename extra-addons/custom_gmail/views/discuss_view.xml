<odoo>
    <!-- Inherit the Discuss Kanban View -->
    <record id="view_inbox_sync_gmail_button" model="ir.ui.view">
        <field name="name">inbox.sync.gmail.button</field>
        <field name="model">mail.channel</field>
        <field name="inherit_id" ref="mail.discuss_channel_view_kanban" />
        <field name="arch" type="xml">
            <!-- Add the Sync Gmail button next to the "Start a meeting" button -->
            <!-- <xpath expr="//button[contains(@class, 'btn-primary') and contains(., 'Start a meeting')]" position="after">
                <button type="object" name="action_sync_gmail" string="Sync Gmail" class="btn btn-secondary o_mail_discuss_button_sync_gmail" />
            </xpath> -->
        </field>
    </record>
</odoo>
